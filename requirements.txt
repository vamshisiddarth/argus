# Full dependency set — used by CI and local dev
# For cloud-specific installs see requirements/
#   AWS only:   pip install -r requirements/aws.txt
#   GCP only:   pip install -r requirements/gcp.txt
#   Azure only: pip install -r requirements/azure.txt
#   All + dev:  pip install -r requirements/dev.txt

-r requirements/dev.txt
