[pytest]
testpaths = tests
pythonpath = .
addopts = --tb=short --timeout=30 --strict-markers -m "not integration"
markers =
    integration: end-to-end tests exercising full adapter→loop→report path with mock data
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
